{
  "name": "Pokemon AI Agent",
  "install": "uv sync && cd ui && pnpm install",
  "start": "echo 'Pokemon AI Agent environment ready. Use terminals to run specific commands.'",
  "terminals": [
    {
      "name": "Pokemon Player",
      "command": "python -m open_llms_play_pokemon.agents.main",
      "description": "Run the main Pokemon AI agent that plays Pokemon Red using vision-language models"
    },
    {
      "name": "DSPy Pokemon Player", 
      "command": "python -m open_llms_play_pokemon.agents.main_dspy",
      "description": "Run the DSPy-based Pokemon AI agent with structured reasoning modules"
    },
    {
      "name": "Run Tests",
      "command": "uv run pytest tests/ -v",
      "description": "Run all tests with verbose output"
    },
    {
      "name": "Test Action Parser",
      "command": "uv run pytest tests/test_action_parser.py -v",
      "description": "Run specific action parser tests"
    },
    {
      "name": "Code Quality Check",
      "command": "./check.sh",
      "description": "Run all code quality checks (format, lint, typecheck)"
    },
    {
      "name": "UI Development",
      "command": "cd ui && pnpm run dev",
      "description": "Start the React UI development server on port 5173"
    },
    {
      "name": "Build UI",
      "command": "cd ui && pnpm run build",
      "description": "Build the React UI for production"
    },
    {
      "name": "Deploy LLM Server",
      "command": "modal deploy inference/llm_server.py", 
      "description": "Deploy the LLM server (UI-TARS-1.5-7B) to Modal cloud"
    },
    {
      "name": "Test LLM Server",
      "command": "modal run inference/llm_server.py::test",
      "description": "Test the deployed LLM server functionality"
    },
    {
      "name": "Format Code",
      "command": "uv run ruff format .",
      "description": "Format code using ruff"
    },
    {
      "name": "Lint Code", 
      "command": "uv run ruff check . --fix",
      "description": "Lint code using ruff with auto-fix"
    },
    {
      "name": "Type Check",
      "command": "uv run pyright",
      "description": "Type check Python code using pyright"
    }
  ]
}